Start Wars Movie app - React App frontend using Typescript

To start the app:
 - clone repo - write in terminal "git clone + <copied repo>"
 - write in terminal "npm i"
 - finally write in terminal "NPM start"
